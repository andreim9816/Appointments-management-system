@model Appointments_management_system.Models.AppointmentRequestModel
@{
    ViewBag.Title = "Edit an appoitment";
}
<h2>@ViewBag.Title</h2>
<br />
<br />

@using (Html.BeginForm(actionName: "Edit", controllerName: "Appointment", method: FormMethod.Post))
{
    @Html.HttpMethodOverride(HttpVerbs.Put)
    @Html.HiddenFor(m => m.AppointmentId)

    @Html.Label("Date", "Change the date:")
    <br />
    @Html.EditorFor(model => model.Date, new { htmlAttributes = new { @class = "datepicker form-control" } })
    @Html.ValidationMessageFor(model => model.Date, "", new { @class = "text-danger" })
    <br />

    @Html.Label("Hour", "Pick an hour:")
    @Html.DropDownListFor(mv => mv.ChosenAppointmentHour, new SelectList(Model.AppointmentHours, "Value", "Value"), new { @class = "form-control" })
    <br />

    @Html.Label("Details", "Details:")
    <br />
    @Html.TextAreaFor(model => model.Details, new { @class = "form-control", @rows = 3 })
    @Html.ValidationMessageFor(model => model.Details, "", new { @class = "text-danger" })
    <br />

    <button class="btn btn-info" type="submit">
        Create
    </button>
}

<link href="~/Content/themes/base/jquery-ui.min.css" rel="stylesheet" />

@section scripts {
    <script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
    <script>
        $(function () {
            $('.datepicker').datepicker({
                dateFormat: "mm/dd/yy",
                changeMonth: true,
                changeYear: true,
                minDate: new Date(),
                defaultDate: new Date(21, 1, 1),
            }).on('change', function (e) {
                let datetime = $(".datepicker").datepicker("getDate").toDateString();
                $.ajax({
                    url: '@Url.Action("GetAllAvailableHours","Appointment")',
                    type: 'GET',
                    data: { date: datetime, doctorId: @Model.DoctorId},
                    success: function (data) {
                        let dropdown = document.getElementById('ChosenAppointmentHour');
                        dropdown.length = 0;
                        console.log(data);

                         for (i = 0; i < data.length; i++) {
                             var opt = new Option(data[i], data[i]);
                             dropdown.append(opt);
                        }
                    }
                })
            })
        });
    </script>
}